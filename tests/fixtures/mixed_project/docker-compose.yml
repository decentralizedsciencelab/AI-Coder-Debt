version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgres://db:5432/app
    depends_on:
      - db

  db:
    image: postgres:14
    environment:
      - POSTGRES_DB=app
      - POSTGRES_PASSWORD=secret
